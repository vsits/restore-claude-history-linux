[tool.pytest.ini_options]
testpaths = ["tests"]
# Layer 3 integration tests are opt-in: run them with `pytest tests/integration/`.
addopts = "--ignore=tests/integration"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
